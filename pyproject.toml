[tool.black]
line-length = 88
target-version = ['py312']

[tool.isort]
profile = "black"

[tool.mypy]
python_version = 3.12
ignore_missing_imports = true

[tool.ruff]
line-length = 88
exclude = [".venv", "venv", "build", "dist"]

[tool.ruff.lint]
extend-select = ["E", "F", "W", "C90"]
